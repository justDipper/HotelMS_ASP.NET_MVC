@{
    ViewData["Title"] = "Home Page";
}


<div class="card text-dark container my-5 py-5 z-depth-1">


    <!--Section: Content-->
    <section >


        <!--Grid row-->
        <div class="row d-flex justify-content-center mb-4">

            <!--Grid column-->
            <div class="col-md-10
">
                @model IEnumerable<LoginAccessModel>
                <a asp-controller="LoginAccess" asp-action="Add" class="btn btn-success d-block mb-3 text-center mx-2 text-decoration-none" type="button">CREATE</a>

                <!-- Inline CSS -->
                <div class="d-flex justify-content-between">
                    <h1 class="text-500 text-dark my-3">Login Access</h1>
                    <div class="mb-3">
                        <input type="text" id="searchInput" class="form-control" placeholder="Search for books..." onkeyup="searchTable()">
                    </div>
                     </div>

                <table id="booksTable" class="custom-table text-dark">
                    <thead>
                        <tr>
                            <th scope="col">Id</th>
                            <th scope="col">Customer ID</th>
                            <th scope="col">Username</th>
                            <th scope="col">Password</th>
                            <th scope="col">Fullname</th>
                            <th scope="col">Birthdate</th>
                            <th scope="col">Address</th>
                            <th scope="col">Gender</th>
                            <th scope="col">Access</th>
                            <th scope="col">Action</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var users in Model)
                        {
                            <tr>
                                <th scope="row">@users.id</th>
                                <td>@users.customer_id</td>
                                <td>@users.username</td>
                                <td>@users.password</td>
                                <td>@users.fullname</td>
                                <td>@users.birth_date</td>
                                <td>@users.address</td>
                                <td>@users.gender</td>
                                <td>@users.access_type</td>
                                <td class="d-flex">
                                    <a asp-controller="LoginAccess" asp-action="Edit" asp-route-id="@users.id" class="btn btn-primary  my-3 text-center mx-2 text-decoration-none" type="button">EDIT</a>
                                    <a asp-controller="LoginAccess" asp-action="Delete" asp-route-id="@users.id" class="btn btn-danger my-3 text-center text-decoration-none" type="button">DELETE</a>
                                </td>   
                            </tr>
                        }
                    </tbody>
                </table>


            </div>
            <!--Grid column-->

        </div>
        <!--Grid row-->


    </section>
    <!--Section: Content-->


</div>
