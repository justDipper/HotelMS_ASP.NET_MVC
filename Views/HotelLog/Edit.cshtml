@model HotelLogModel

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-dark text-white">
                    <h1 class="text-center">Edit User Account</h1>
                </div>
                <div class="card-body">
                    <form method="post" asp-controller="HotelLog" asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="id" />
                        <div class="mb-3">
                            <label asp-for="customer_id_fk" class="form-label">Customer ID</label>
                            <input asp-for="customer_id_fk" type="text" class="form-control"  />
                            <span asp-validation-for="customer_id_fk" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="room_id_fk" class="form-label">Room ID</label>
                            <input asp-for="room_id_fk" type="text" class="form-control"  />
                            <span asp-validation-for="room_id_fk" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="status" class="form-label">Status</label>
                            <input asp-for="status" type="text" class="form-control"  />
                            <span asp-validation-for="status" class="text-danger"></span>
                        </div>

                        <div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header bg-dark text-white">
                                        <h5 class="modal-title" id="exampleModalLabel">Update Confirmation</h5>
                                        <button type="button" class="btn-close" data-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        Are you sure you want to update this item?
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                        <button type="submit" class="btn btn-danger">Yes</button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <button class="btn btn-primary btn-block" type="submit" data-toggle="modal" data-target="#deleteModal">Update</button>
                        </div>
                    </form>
                    <a href="javascript:history.back()" class="text-center d-block">← Go Back</a>
                </div>
            </div>
        </div>
    </div>
</div>
